
   /* String[] jugadorsubcap = new String[nombresequipo1.length - 1];
    int indicejugadorsubcap = 0;
    
    for (int i = 0; i < nombresequipo1.length; i++) {
       
        if (nombresequipo1[i] != " Pedro López ") {
            jugadorsubcap[indicejugadorsubcap] = nombresequipo1[i];
            indicejugadorsubcap++;
        }
    }
    
    
    for (int i = 0; i < jugadorcap.length; i++) {
        System.out.println(jugadorsubcap[i]);
    }  */
 String[] jugadorsubcap = new String[nombresequipo2.length - 1];
int indicejugadorsubcap = 0;

for (int i = 0; i < nombresequipo2.length; i++) {
   
    if (nombresequipo2[i] != "Rafael Reyes") {
        jugadorsubcap[indicejugadorsubcap] = nombresequipo2[i];
        indicejugadorsubcap++;
    }
}


for (int i = 0; i < jugadorcap.length; i++) {
    System.out.println(jugadorsubcap[i]);
}


 String[] jugadorsubcap = new String[nombresequipo3.length - 1];
        int indicejugadorsubcap = 0;
        
        for (int i = 0; i < nombresequipo3.length; i++) {
           
            if (nombresequipo3[i] != "Raúl León") {
                jugadorsubcap[indicejugadorsubcap] = nombresequipo3[i];
                indicejugadorsubcap++;
            }
        }
        
        
        for (int i = 0; i < jugadorcap.length; i++) {
            System.out.println(jugadorsubcap[i]);
        }